# AIエージェント運用ガイド

## リポジトリ概要
- 個人用 dotfiles リポジトリ。リポジトリ直下の `./home/` 配下の設定をホームディレクトリへシンボリックリンクで展開する。

## 編集ルール
- 設定ファイルは必ずリポジトリ内 `./home/` 配下のソースを編集し、ホームディレクトリ側のリンク先は触らない。

## スクリプト
- `./run <スクリプト名>` で `scripts/` 配下のスクリプトを実行。AI は実行しない（必要ならユーザーに案内する）。
  - `install`: 管理ツールの初回セットアップ/更新
  - `sync`: `./home/` の内容をホームディレクトリへシンボリックリンク

## 詳細ドキュメント
- 手順や背景情報は `README.md` を参照。
